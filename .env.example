# Distyl Intel Portal — Environment Variables
# Copy this to .env and fill in your values

# ── Core ─────────────────────────────────────────────────────────
SECRET_KEY=your-secret-key-here-change-in-production
FLASK_ENV=development
FLASK_DEBUG=false
PORT=5002

# ── Database ─────────────────────────────────────────────────────
# Leave blank for SQLite (local dev), set for PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@host:5432/distyl_intel

# ── Anthropic (REQUIRED) ─────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Google OAuth (for login + Gmail + Drive) ─────────────────────
# Create at: console.cloud.google.com → APIs & Services → Credentials
# Enable: Gmail API, Drive API, People API
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5002/auth/google/callback

# ── Allowed email domain ─────────────────────────────────────────
# Users must have @distyl.ai Google accounts to log in
# Set to 'any' to allow any Google account (dev/testing only)
ALLOWED_EMAIL_DOMAIN=distyl.ai

# ── Slack ─────────────────────────────────────────────────────────
# Create at: api.slack.com/apps → Bot Token Scopes: chat:write, commands, users:read
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...
SLACK_INTEL_CHANNEL=#competitive-intel

# ── Gmail Push Notifications (Pub/Sub) ───────────────────────
# Create at: console.cloud.google.com → Pub/Sub → Topics
GMAIL_PUBSUB_TOPIC=projects/YOUR_PROJECT/topics/gmail-push
GMAIL_PUBSUB_TOKEN=your-random-verification-token

# ── News Sources ─────────────────────────────────────────────────
# NewsAPI: newsapi.org/register (free tier: 100 requests/day)
NEWSAPI_KEY=your-newsapi-key

# Perplexity: perplexity.ai/settings/api (paid)
PERPLEXITY_API_KEY=pplx-...

# ── Frontend ─────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173
